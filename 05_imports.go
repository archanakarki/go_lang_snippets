// grouping the imports into () or parenthesized import stmt
// Use Printf instead of Println while using
package main

import (
	"fmt"
	"math"
)

func main(){
	fmt.Printf("Now you have %g problems. \n", math.Sqrt(7))
}